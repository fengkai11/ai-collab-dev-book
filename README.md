# AI 协同开发：切片式开发训练手册

这是基于 `mdBook` 的在线图书项目，正文来自 `ai_collab_slice_book_chapters_v0.3_full_draft_balanced`。

## 本地预览

```bash
/home/fengkai/.cargo/bin/mdbook serve --open
```

## 本地构建

```bash
/home/fengkai/.cargo/bin/mdbook build
```

构建产物位于 `book/` 目录。GitHub Pages 发布由 `.github/workflows/pages.yml` 自动完成。
